<form method="post" action="{{ path('app_ingredient_delete', {'idIng': ingredient.idIng}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ingredient.idIng) }}">
    <button class="btn">Delete</button>
</form>
