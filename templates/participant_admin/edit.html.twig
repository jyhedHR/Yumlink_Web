{% extends 'adminBase.html.twig' %}

{% block title %}Modifier Participant{% endblock %}

{% block body %}
<div class="main-container">
        <div class="pd-ltr-20 xs-pd-20-10">
            <div class="min-height-200px">
    <h1>Modifier Participant</h1>

    {{ include('participant_admin/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_participant_admin_index') }}">back to list</a>

    {{ include('participant_admin/_delete_form.html.twig') }}
     </div>
            </div>
             </div>
{% endblock %}
