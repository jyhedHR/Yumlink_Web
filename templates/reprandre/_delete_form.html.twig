<form method="post" action="{{ path('app_reprandre_delete', {'id': reprandre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reprandre.id) }}">
    <button class="btn">Delete</button>
</form>
