{% extends 'base.html.twig' %}

{% block title %}Reprandre{% endblock %}

{% block body %}
    <h1>Reprandre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reprandre.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ reprandre.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reprandre_index') }}">back to list</a>

    <a href="{{ path('app_reprandre_edit', {'id': reprandre.id}) }}">edit</a>

    {{ include('reprandre/_delete_form.html.twig') }}
{% endblock %}
