{% extends 'base.html.twig' %}

{% block title %}Recettes{% endblock %}

{% block body %}
    <h1>Recettes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdR</th>
                <td>{{ recette.idR }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ recette.nom }}</td>
            </tr>
            <tr>
                <th>Chef</th>
                <td>{{ recette.chef }}</td>
            </tr>
            <tr>
                <th>Ingredients</th>
                <td>{{ recette.ingredients ? recette.ingredients|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td>{{ recette.categorie }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ recette.description }}</td>
            </tr>
            <tr>
                <th>Imgsrc</th>
                <td>{{ recette.imgsrc }}</td>
            </tr>
            <tr>
                <th>Calorie</th>
                <td>{{ recette.calorie }}</td>
            </tr>
            <tr>
                <th>Protein</th>
                <td>{{ recette.protein }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_recettes_index') }}">back to list</a>

    <a href="{{ path('app_recettes_edit', {'idR': recette.idR}) }}">edit</a>

    {{ include('recettes/_delete_form.html.twig') }}
{% endblock %}
