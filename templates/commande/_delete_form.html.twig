<form method="post" action="{{ path('app_commande_delete', {'idCom': commande.idCom}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commande.idCom) }}">
    <button class="btn">Delete</button>
</form>
